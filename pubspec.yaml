name: trashapp
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  
  # Picking Between the Video and Picture
  image_picker: ^1.2.0

  # Self explanatory
  camera: ^0.11.2 

  # Plays video
  video_player: ^2.10.0

  # Map View
  geolocator: ^14.0.2

 
dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0


flutter:

  uses-material-design: true

